# Hymac自用HDT 2.2

本版底座更新至 **HSTracker 3.6.7**，并同步最新炉石卡牌数据。

![炉石传说酒馆战棋第 14 赛季](https://raw.githubusercontent.com/hymac731/Hymac-HDT/master/assets/battlegrounds-season-14.png)

## 更新内容

- 修正卡牌数据异常可能导致的覆盖层缺失等问题。
- 合入 HSTracker 3.6.7 的覆盖层、酒馆战棋、Bob's Buddy 和双打模式相关修复。
- 包含应用稳定性、战绩面板与其他界面细节修复。
- 保留 Hymac 一键拔线、连接自动识别、战绩面板移动缩放和最近 8 场战绩等功能。

## 安装或升级

打开 DMG，把 `Hymac自用HDT.app` 拖进“应用程序”并选择替换，然后在 DMG 中右键 `① 本地重签（安装后跑一次）.command`，选择“打开”并等待完成。

本地重签只需在首次安装或替换新版后运行一次，日常打开同一版本不需要重复重签。重签不能自动开启 macOS 隐私权限；如果只看到红色“拔线”按钮，请重新检查“屏幕与系统音频录制”权限并重启 App。

---

# Hymac HDT 2.2

Updated on top of **HSTracker 3.6.7** with the latest Hearthstone card data.

## Changes

- Corrects card-data issues that could cause missing overlays and related display problems.
- Includes upstream overlay, Battlegrounds, Bob's Buddy, and Duos fixes from HSTracker 3.6.7.
- Includes application stability, session-panel, and other interface fixes.
- Retains Hymac reconnect, automatic connection matching, movable/resizable session panel, and up to 8 recent games.

## Install or Upgrade

Open the DMG, drag `Hymac自用HDT.app` into Applications and choose Replace. Then right-click `① 本地重签（安装后跑一次）.command` inside the DMG, choose Open, and wait for completion.

Run the local re-sign step only once after the first installation or after replacing the app with a newer version. Daily launches of the same version do not require another re-sign. The script cannot enable macOS privacy permissions; if only the red Reconnect button appears, verify Screen & System Audio Recording permission and restart the app.
